# ripple

CLI tool to download and install Proton builds into one central store, then symlink them into Steam/Bottles/Lutris paths.

## Install

```bash
pip install .
```

## Usage

```bash
ripple --help
```

Config file: `~/.config/ripple/config.json`.
